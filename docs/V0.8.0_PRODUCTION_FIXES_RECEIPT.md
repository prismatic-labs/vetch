# v0.8.0 production audit — fix receipt (for Claude Code)

All 24 ranked audit items were addressed in this pass. Summary by area.

## Python — calibration & energy (`src/vetch/`)

| # | Issue | Fix |
|---|--------|-----|
| 1 | VLM double-count visual tokens in text energy | `calculate_energy` uses `_effective_text_input_tokens()` when `energy_override` has `visual_tokens_per_image` |
| 2 | No Ollama instrumentation | New `src/vetch/providers/ollama.py`; wired in `instrument()` / `uninstrument()` |
| 3 | Exact model string for `load_calibration` | `calibration_model_variants()` + alias lookup (`moondream` ↔ `moondream:latest`) |
| 7 | Invalid Apple cal exits 0 | `CalibrationResult.active` / `rejection_reasons`; CLI `sys.exit(1)` when inactive |
| 12 | Empty `calibrations.json` unused | `community_calibrations.py` + bundled `vetch/data/community_calibrations.json` (force-included from `data/calibrations.json`); fallback in `_get_local_calibration` |
| 13 | `residuals_structured` never computed | `_detect_residuals_structured()` in `_fit`; written to detail JSON |
| 14 | NumPy optional weak fits | `_require_numpy_for_calibration()` already enforced at start of `calibrate_apple_silicon` |
| 15 | Silent grid failures | Track `grid_failures`; fail if >50% failed, warn if >25% |
| 16 | NVIDIA heuristic mislabeled | Tier 1 label + explicit method line in `format_calibration_result` |
| 17 | Powermetrics vs wall | Startup + result notes; `measurement_basis` / `not_wall_power` in detail JSON (unchanged, clarified in UI text) |
| 18 | Thermal idle drift | Abort with `RuntimeError` when `idle_drift_pct > 15%` before fit |
| 22 | `intercept_wh` semantics | Documented in `format_calibration_result_apple` |
| 23 | Wikimedia partial download | `download_calibration_images(strict=True)` + `--strict-images` CLI |

## Vercel AI SDK (`packages/vetch-ai-sdk/`)

| # | Issue | Fix |
|---|--------|-----|
| 4 | `usage.image` always null | `countImagesInParams` + `createUsage` sets `image_count` |
| 5 | No local cal auto-load | `local-calibration.ts` + `enrichVetchEvent` loads `~/.vetch` on Node |
| 6 | Same text-token mismatch | `effectiveTextInputTokens()` in TS `calculateEnergy` override branch |
| 8 | Emitter timeout silent drop | `withTimeout` throws; surfaces via `onEmitterError` / fail-open |
| 9 | Registry drift | `scripts/sync_ai_sdk_registries.py` + CI step in `ai-sdk` job |
| 10 | Version skew | `src/version.ts` → `VETCH_VERSION = "0.8.0"`; package `0.8.0` |
| 11 | `private: true` / install story | `README.md` + `packages/vetch-ai-sdk/README.md` dual-install section |
| 19 | Session LRU silent | `onSessionEvicted` callback + debug warning on eviction |
| 20 | Gateway parsing | `parseGatewayModelId()` with known-provider allowlist |
| 21 | Fetch emitter ops | `bearerToken`, `retries` on `createFetchEmitter` |

## Cross-cutting

| # | Issue | Fix |
|---|--------|-----|
| 24 | PyPI wheel vs npm package | Root `README.md` “0.8.0 release train” section |

## Tests added/updated

- `tests/test_calibrate_metal.py` — alias load, visual-token subtraction
- `tests/test_ollama_provider.py` — Ollama helpers + instrument guard
- `tests/test_advisory_new.py` — CACHE-002, STREAM-001, REASONING-001, ERROR-001, TRUNC-001 on "length", on_advisory callback

## Post-tag second pass (all landed in v0.8.0)

| Item | Description | Status |
|------|-------------|--------|
| CACHE-002 advisory | High repetition + no cache reads | ✅ Done |
| STREAM-001 advisory | High incomplete-stream fraction | ✅ Done |
| REASONING-001 advisory | Reasoning model, no reasoning tokens | ✅ Done |
| TRUNC-001 fix | Also fires on `finish_reason="length"` (OpenAI) | ✅ Done |
| `retry_count` field | Emitted as 0; PREMIUM-001 gate inert until callers set it | ✅ Done |
| Ollama provider detection | `localhost:11434` → `provider="ollama"` in openai.py | ✅ Done |
| AI SDK STALL-001/CACHE-001/002 | Session advisories in advisories.ts + 7 vitest tests | ✅ Done |
| `_RecentCall` rollup | 5 new fields: cache_read_tokens, is_stream, complete, is_reasoning_model, has_reasoning_tokens | ✅ Done |
| CI: sdist missing /data | Added `/data` to sdist include in pyproject.toml | ✅ Done |
| CI: npm lockfile | Regenerated package-lock.json (@types/node, undici-types) | ✅ Done |
| Wheel duplicate warning | Removed redundant `force-include` (file already in src/vetch/data/) | ✅ Done |
| METHODOLOGY → 1.2 | Added VLM image energy term, intercept_wh, Apple Silicon calibration | ✅ Done |
| CHANGELOG complete | All v0.8.0 items documented | ✅ Done |
| npm publish | Set `"private": false` and add release workflow | 🔜 Deferred |
| vLLM hooks | Native vLLM instrumentation | 🔜 Deferred |
| Community calibrations fill | `scripts/aggregate_calibrations.py` + submitted PRs | 🔜 Deferred |
| RETRY-001 detector | Wire `retry_count` to advisory engine | 🔜 Deferred |

## Verify locally

```bash
pip install -e ".[test,dev,apple-silicon]"
pytest tests/ -q --no-cov
python scripts/sync_ai_sdk_registries.py
npm --prefix packages/vetch-ai-sdk run check
npm --prefix packages/vetch-ai-sdk test
```

## Notes for follow-up (not blockers)

- Publish `@vetch/ai-sdk` to npm when ready: set `"private": false` and add release workflow.
- Refresh `packages/vetch-ai-sdk/src/registry/*.json` whenever Python registries change (CI will fail until synced).
- Populate `data/calibrations.json` via `scripts/aggregate_calibrations.py` to fill community priors.
- `retry_count` is emitted as `0` by default. PREMIUM-001 retry-rate gate activates only when callers explicitly set `retry_count > 0`. This is by design — document in your app's vetch integration if you perform retries.
