# Required for the sample route (OpenAI via AI SDK)
OPENAI_API_KEY=

# Optional: POST schema v2 events to your collector (see createFetchEmitter)
VETCH_EVENTS_URL=
VETCH_COLLECTOR_TOKEN=

# Optional: emergency off / per-call budget thresholds (Node)
# VETCH_DISABLED=true
# VETCH_BUDGET_COST_USD=0.50
