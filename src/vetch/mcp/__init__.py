"""Vetch MCP server — Model Context Protocol interface for LLM observability.

Install with: pip install vetch[mcp]

The tools and resources submodules are stdlib-only and can be imported without
the ``mcp`` package (useful for testing). Only ``server.py`` requires the MCP SDK.
"""

from __future__ import annotations
